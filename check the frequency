# Sample test dictionary
test_dict = {
    'a': 10,
    'b': 20,
    'c': 10,
    'd': 30,
    'e': 10
}

# Value to check frequency for
target_value = 10

# Count frequency using list comprehension
frequency = sum(1 for v in test_dict.values() if v == target_value)

print(f"Frequency of value {target_value}:", frequency)
