# Get user input
rows = int(input("Enter the number of rows: "))

# Generate mirrored right-angled triangle
for i in range(1, rows + 1):
    spaces = ' ' * (rows - i)
    stars = '*' * i
    print(spaces + stars)
